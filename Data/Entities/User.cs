// User and Administrator entities removed — app runs without authentication.
// League captain contact info is stored in AppParameter (LeagueCaptainName, LeagueCaptainEmail).
namespace BocceManager.Data.Entities;
