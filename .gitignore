# Build output
bin/
obj/

# Visual Studio / VS Code
.vs/
*.user
*.suo
.vscode/

# SQLite database (data stays local, not in source control)
*.db
*.db-shm
*.db-wal

# Postgres backups (contain real player/contact data, not for source control)
db_backups/

# Database connection config (contains Neon credentials — distributed manually
# alongside the .exe, never committed)
db.config

# Theme preference (local setting, not shared)
bocce-theme.txt

# FastReport designer temp files
*.bak

# OS
Thumbs.db
Desktop.ini
.DS_Store

# NuGet
*.nupkg
project.lock.json
